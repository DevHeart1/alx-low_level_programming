#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase character.
 * @c: character to be read.
 * Return: 0 and 1.
 */

int _isupper(int c)
{
	if (c >= 'A' && C <= 'Z')
		return (1);
	else
		return (0);
}
