#include "main.h"

/**
 * mul - multiplies two integers.
 * @a,b - character
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int i = a*b

	if ((a >= '0' || a <= '0') && (b >= '0' || b <=0))
		_putchar("%d", i);
return (0);
}
